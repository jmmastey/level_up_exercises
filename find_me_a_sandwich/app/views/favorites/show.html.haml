.page-header
  %h1 Favorites

- if user_signed_in?
  - if @favorites.length == 0
    %p Save some meals to favorites and they will show up here after.
  - else
    %table{ class: "table" }
      %thead
        %tr
          %th{ colspan: "2" } Name
      %tbody
        - @favorites.each do |favorite|
          %tr
            %td= favorite.menu_item.name
            %td= favorite_link(favorite.menu_item)
- else
  %p Login or signup to start saving favorites!