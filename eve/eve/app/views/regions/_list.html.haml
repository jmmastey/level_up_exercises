= paginate @regions
%table.table.table-condensed.table-striped
  %thead
    %tr
      %th EVE ID
      %th Name
      %th
  %tbody
    - @regions.each do |region|
      %tr
        %td= region.in_game_id
        %td= region.name
        %td
= paginate @regions
