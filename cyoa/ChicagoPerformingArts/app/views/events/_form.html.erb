<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :placeName %><br>
    <%= f.number_field :placeName %>
  </div>
  <div class="field">
    <%= f.label :cityName %><br>
    <%= f.text_area :cityName %>
  </div>
  <div class="field">
    <%= f.label :activityStartDate %><br>
    <%= f.text_area :activityStartDate %>
  </div>
  <div class="field">
    <%= f.label :activityEndDate %><br>
    <%= f.text_area :activityEndDate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
