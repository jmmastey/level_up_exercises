Given(/^I am on the settings page$/) do
  step "I am logged in"
  click_button "edit_user_link"
end

When(/^I add a notification$/) do
  pending # express the regexp above with the code you wish you had
end

Then(/^I see the saved notification$/) do
  pending # express the regexp above with the code you wish you had
end