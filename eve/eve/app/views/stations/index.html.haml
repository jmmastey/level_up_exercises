.page-header
  %h2 EVE Online Stations

= form_tag("/stations/search", method: "get", role: "form") do
  .form-group
    = label_tag(:query, "Search", class: "sr-only")
    .input-group
      = search_field_tag(:query, @query, class: "form-control", placeholder: "Search stations")
      .input-group-btn
        %button{type: "submit", class: "btn btn-default", title: "Search"}
          &nbsp;
          %span.glyphicon.glyphicon-search
          &nbsp;

- if @stations && !@stations.empty?
  = render(partial: 'list', locals: { regions: @regions })
- else
  %p.alert.alert-info
    Could not find any stations.
