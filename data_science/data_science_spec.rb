require_relative 'data_science'

describe Optimizer do
  fake_data = [{},{}]
  before :each do
    @optimizer = Optimizer.new fake_data
  end

  describe it "counts total sample size per cohort" do

  end

  describe it "counds number of conversions per cohort" do

  end 
end