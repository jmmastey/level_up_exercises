<% content_for :legislators_header do %>
  <h4>Legislators (Favorites Only)</h4>
  <% if user_signed_in? %>
    <div class="favorite-toggle">
      <%= link_to "Show All", legislators_path %>
    </div>
  <% end %>
<% end %>

<%= render :partial => "legislators_base" %>
