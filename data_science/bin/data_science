#!/usr/bin/env ruby

require_relative '../lib/data_science'

raise ArgumentError, "No file given" unless (file = ARGV[0])

data = DataScience::WebsiteData.new(file)
stats = DataScience::Stats.new(data)

puts stats.to_s
