<%= "Bill:" %>
<% if bill %>
  <%= raw "#{link_to bill.short_title, bill}" if bill.short_title %>
  <%= raw "#{link_to bill.bill_id, bill}" unless bill.short_title %>
<% end %>
<%= "#{deed.bill_id}" unless bill %>