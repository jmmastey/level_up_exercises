<h1>Opposite of Progress</h1>

<p><%= link_to "View all Legislators", legislators_path %></p>

<p><%= link_to "View all Bills", bills_path %></p>

<h2>All Good Deeds</h2>

<% @results.each do |result| %>
  <% @deed = result %>
  <%= render partial: "deed_list_item", locals: {deed: @deed} %>
<% end %>

<%= "There are no good deeds to show" if @results.count == 0 %>
