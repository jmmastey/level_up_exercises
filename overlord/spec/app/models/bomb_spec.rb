require "spec_helper"
require_relative APP_PATH + "/models/bomb"

RSpec.describe Bomb do

  describe "has a tautological test" do
    # let(:bomb)  {Bomb.new(activation_code: 1234, deactivation_code: 1234)}
    # let(:bomb2) {double("bomb", activation_code: 1234) }
    it '...playing around....?' do
      # expect(:bomb2).to receive(:activation_code).return(1234)
      # allow(book).to receive(:title).and_return("The RSpec Book")

      # expect(:bomb2.activation_code).to eq(1234)
    end

  end
end
