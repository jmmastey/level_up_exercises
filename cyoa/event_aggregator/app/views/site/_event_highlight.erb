<div class="event-highlight thumbnail"> 
  <h4><%= event.title %></h4>
  <img class="event-image" src="<%= event.image_url || image_path("calendar.svg")%>">
  <div class="caption">
    <h5>Description</h5>
    <p><%= event.description %></p>
  </div>
</div>
